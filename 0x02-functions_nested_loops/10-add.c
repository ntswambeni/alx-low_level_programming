#include "main.h"

/**
 * add - sums two integers and returns the result
 * @m: First integer param
 * @n: Second integer param
 * Return: sum of the params
 */
int add(int m, int n)
{
	int sum;

	sum = m + n;
	return (sum);
}
