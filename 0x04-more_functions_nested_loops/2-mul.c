/**
  * mul - multiplies two integers and returns the result
  * @a: first integer param
  * @b: second integer param
  * Return: a * b
  */
int mul(int a, int b)
{
	int sum;

	sum = a * b;
	return (sum);
}
